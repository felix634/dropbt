---
// IMPORTS CORRECTED: 4 levels deep requires '../../../'
import MainLayout from '../../../layouts/MainLayout.astro';
import { getLangFromUrl, useTranslations } from '../../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<MainLayout title={`${t('header.communication_design')} | ${t('header.project_flow')}`}>
    <section class="container mx-auto px-6 py-20">
        <h1 class="text-4xl font-extrabold text-gray-800 mb-6 animate-fade-in-down">
            {t('header.project_flow')}
        </h1>
        <p class="text-lg text-gray-600 mb-10 animate-fade-in-up-delay">
            Our structured, transparent process ensures efficient collaboration and high-quality results from start to finish.
        </p>

        <div class="max-w-4xl mx-auto">
            <ol class="space-y-12 relative border-l-4 border-drop-red pl-8">
                <li class="animate-fade-in-up">
                    <div class="absolute w-5 h-5 bg-drop-red rounded-full -left-2.5 mt-1.5 border-4 border-white shadow-md"></div>
                    <h2 class="text-3xl font-bold text-gray-800 mb-1">1. Discovery & Briefing</h2>
                    <p class="text-gray-600">We start with an in-depth consultation to understand your brand, define the project scope, identify key goals, and establish the target audience for clear alignment.</p>
                </li>
                <li class="animate-fade-in-up-delay">
                    <div class="absolute w-5 h-5 bg-drop-red rounded-full -left-2.5 mt-1.5 border-4 border-white shadow-md"></div>
                    <h2 class="text-3xl font-bold text-gray-800 mb-1">2. Strategy & Concept Development</h2>
                    <p class="text-gray-600">Based on the brief, we develop the creative concept, detailed project strategy, initial wireframes, and mood boards. This is where the foundation of the design is set.</p>
                </li>
                <li class="animate-fade-in-up-delay-2">
                    <div class="absolute w-5 h-5 bg-drop-red rounded-full -left-2.5 mt-1.5 border-4 border-white shadow-md"></div>
                    <h2 class="text-3xl font-bold text-gray-800 mb-1">3. Execution & Refinement</h2>
                    <p class="text-gray-600">Our team executes the design, develops all necessary assets (copy, graphics, code), and continuously refines the work based on internal quality checks and initial feedback cycles.</p>
                </li>
                <li class="animate-fade-in-up-delay-2">
                    <div class="absolute w-5 h-5 bg-drop-red rounded-full -left-2.5 mt-1.5 border-4 border-white shadow-md"></div>
                    <h2 class="text-3xl font-bold text-gray-800 mb-1">4. Delivery & Launch</h2>
                    <p class="text-gray-600">After final client approval, rigorous testing is completed across all devices and browsers. We then proceed with the official launch and provide post-launch support.</p>
                </li>
            </ol>
        </div>
    </section>
</MainLayout>