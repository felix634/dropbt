---
// IMPORTS CORRECTED: 4 levels deep requires '../../../'
import MainLayout from '../../../layouts/MainLayout.astro';
import { getLangFromUrl, useTranslations } from '../../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<MainLayout title={`${t('header.communication_design')} | ${t('header.partners')}`}>
    <section class="container mx-auto px-6 py-20">
        <h1 class="text-4xl font-extrabold text-gray-800 mb-6 animate-fade-in-down">
            {t('header.partners')}
        </h1>
        <p class="text-lg text-gray-600 mb-10 animate-fade-in-up-delay">
            We collaborate with industry leaders and specialized agencies to provide comprehensive solutions.
        </p>

        <div class="flex flex-wrap justify-center items-center gap-10 bg-white p-10 rounded-lg shadow-xl animate-fade-in-up-delay-2">
            <div class="w-32 h-20 flex items-center justify-center border border-gray-300 rounded-md text-gray-500 font-semibold">Design Studio</div>
            <div class="w-32 h-20 flex items-center justify-center border border-gray-300 rounded-md text-gray-500 font-semibold">Tech Agency</div>
            <div class="w-32 h-20 flex items-center justify-center border border-gray-300 rounded-md text-gray-500 font-semibold">SEO Experts</div>
            <div class="w-32 h-20 flex items-center justify-center border border-gray-300 rounded-md text-gray-500 font-semibold">Content Writer</div>
        </div>
    </section>
</MainLayout>